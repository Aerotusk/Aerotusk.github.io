Fade-in animation for the planets (or, finally add in a "landing" page to force interaction so the planet hover sound can play)

Make ellipses fade out in retrograde direction (dotted line with evenly spaced dots, each one getting progressively dimmer until a floor value)
    Nevermind on the dots/dashes, they look awful. Still might be able to do the dimming using a bunch of circles, though
    + Use conic gradients, relatively new to CSS

Add some quiet sounds on hover over planets (Not working atm, "promise" to not play sounds until interaction but hover doesn't count :/ )
    Need to implement a "click to enter" on Sun, then also better support forward/back navigation

Add moving shadows to the planets? Somehow idk
    Maybe rotate the planets as they go around the sun? Definitely rotate the Interloper so it's more accurate

Make the solar system rotatable (stretch goal)

Mobile version of the site LMAOOOOO

Add velocity to planet/list animations so they feel a bit more natural?

Support no JS/no animations (planets in "correct" starting positions before the animation even starts, using PHP)

Reduce fps of all planets [except interloper] (make this a setting/cookie, like site animation *MAXIMUM* framerate = off(don't animate?) low (10fps) medium (30fps) high (60fps) custom (xFPS?))
    animation off should just use the ::hover attribute for planets to change their size
    Also check for prefers-reduced-motion css media feature to automatically set animation rate if there's no cookie saved

(Honestly just give the settings tab a facelift) -> Make custom checkboxes and dropdowns to match the website theme

Test a different space background color for lower contrast? Maybe also include more stars?

Add an accessibility page with a form to request other types of accessibility, tell me what types are/have been most helpful, etc.

Look into using a sprite sheet for rendering the planets

Turn .css files into .php and save colors as variables to be used by other files? Would make footer icon coloring a bit easier (but not sure if it would make everything else easier)